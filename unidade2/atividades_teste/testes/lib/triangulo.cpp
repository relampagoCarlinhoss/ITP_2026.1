/**
 * Verifica o tipo de triângulo de acordo com a dimensão de seus lados.
 * Retorna um valor inteiro que representa o tipo do triângulo, sendo?
 * - 0: se as dimensões não formarem um triângulo (a soma de dois lados for menor ou igual ao terceiro lado).
 * - 1: se for um triêngulo escaleno (todos os lados diferentes), ou;
 * - 2: se for um triângulo isósceles (dois lados iguais);
 * - 3: se for um triângulo equilátero (todos os lados iguais);
 * 
 * @param l1 Dimensão do primeiro lado
 * @param l2 Dimensão do segundo lado
 * @param l3 Dimensão do terceiro lado
 * @return Retorna 4 possíveis valores, em função do tipo de triângulo.
 */
int triangle_type(double l1, double l2, double l3) {
  // a completar
  return 2;
}